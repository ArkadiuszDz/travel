import React from 'react';
import * as Styled from './styles';

const Header: React.FC = () => {

  return (
    <Styled.Header>
      <div>
        TRAVEL
      </div>
    </Styled.Header>
  );
};

export default Header;